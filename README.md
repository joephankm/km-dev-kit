# km-dev-kit

Personal development toolkit — reusable scripts, configs, and patterns.

## Node Scripts

TypeScript scripts runnable with [`tsx`](https://tsx.is). No compile step needed.

**Run:** `pnpm exec tsx scripts/<path>.ts`

| Name       | Type    | Description                           |
|------------|---------|---------------------------------------|
| `hello.ts` | Example | Greets the author from `package.json` |
